/**
 * Shared Prettier configuration
 * Consistent formatting across the monorepo
 */
const config = {
  semi: false,
  singleQuote: true,
  tabWidth: 2,
  trailingComma: 'es5',
  printWidth: 100,
  arrowParens: 'always',
}

export default config;
