/**
 * Shared TypeScript types for all apps
 * Pure type definitions - zero runtime code
 */

// Auth types
export type { AuthUser, JWTPayload } from './auth'

// API types
export type { ApiResponse, PaginatedResponse } from './api'

// Finance types
export type { Currency } from './finance'